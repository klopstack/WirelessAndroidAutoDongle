verbose = 1;

globals :
{
	/*
	 * We only use SWUpdate as an *installer* (invoked by our OTA helper).
	 * Suricatta/daemon mode can be enabled later if desired.
	 */
};

download :
{
	/*
	 * OTA helper downloads the bundle to /persist/ota/update.swu.
	 * This file exists mainly as a placeholder for future expansion.
	 */
};

